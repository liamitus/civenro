services:
  db:
    image: postgres:17-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: civenro
      POSTGRES_PASSWORD: civenro
      POSTGRES_DB: civenro_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
