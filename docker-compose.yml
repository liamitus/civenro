services:
  db:
    image: postgres:17-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: govroll
      POSTGRES_PASSWORD: govroll
      POSTGRES_DB: govroll_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
